[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "build/client"

[context.production]
  command = "npm run build"
  environment = { NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
